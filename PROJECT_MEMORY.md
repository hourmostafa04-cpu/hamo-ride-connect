# PROJECT_MEMORY — MOL TRANSPORT / مول طرانسبور

> ذاكرة دائمة للمشروع. تُقرأ قبل أي تعديل. لا يُغيَّر أي قرار مذكور هنا إلا بطلب صريح من المالك (Mustafa).

## 1. التطبيق
- **الاسم الظاهر:** MOL TRANSPORT / مول طرانسبور (الاسم القديم: حمولة — لم يعد يُستعمل في الواجهة).
- **الرابط الثابت المنشور:** https://hamo-ride-connect.lovable.app
- **الهدف:** ربط مباشر بين صاحب البضاعة وصاحب الشاحنة في المغرب: نشر طلب نقل، استقبال عروض من السائقين، قبول عرض، تتبع الرحلة على الخريطة، وتواصل عبر شات ورسائل صوتية.
- **اللغة:** عربية/دارجة مغربية، اتجاه RTL، واجهة موجهة للهاتف.
- **التقنية:** TanStack Start + React + Tailwind v4، Lovable Cloud (Supabase) للبيانات والمصادقة.

## 2. الأدوار (فصل صارم)
### صاحب البضاعة (Shipper)
- ينشئ طلب نقل (انطلاق، وجهة، نوع الشاحنة، ملاحظات، سعر/مسافة).
- يرى **أصحاب الشاحنات فقط**، مرتبين حسب المسافة (الأقرب أولاً) مع إظهار المسافة بوضوح.
- في «طلباتي» يرى السائقين الذين قدّموا عرضاً لكل طلب مع زرّي قبول/رفض (acceptBid / declineBid).
- **ممنوع** إرجاع «صفحة العروض القديمة» المنفصلة.

### صاحب الشاحنة (Driver)
- يرى **طلبات أصحاب البضاعة فقط**، مرتبة حسب القرب.
- لا تظهر له قائمة شاحنات أخرى إطلاقاً.
- يقدّم عرضاً، ويتابع عروضه في «عروضي».

### قاعدة عامة
صاحب البضاعة يرى الشاحنات، وصاحب الشاحنة يرى الطلبات. لا خلط بين الدورين في أي شاشة أو في استئناف المسار.

## 3. الوظائف الحالية
- تسجيل/دخول بالهاتف عبر SMS/OTP (Supabase Phone Auth + Vonage).
- **وضع تجريبي:** رقم واحد فقط `0600000000` يتخطى SMS، وزر «دخول تجريبي» يظهر في التطوير فقط (`VITE_DEMO_LOGIN`). لا رقم آخر يتخطى OTP.
- إنشاء ونشر الطلبات (`publishLoad` يحفظ في قاعدة البيانات قبل اعتبار الطلب منشوراً، ولا يبتلع الخطأ).
- عروض (bids): تقديم، قبول، رفض، إشعار `bid-answer`.
- شات حقيقي بين طرفَي الطلب فقط (`chat_messages` + bucket `chat-voice`).
- Web Push (PWA، `public/sw.js`، جدول `push_subscriptions`) لإشعارات: طلب جديد، عرض جديد، رسالة شات، رد على عرض.
- خرائط Google Maps + GPS + تتبع مباشر مع حركة سلسة للماركر ومسار وسرعة وETA وزر «إنهاء الرحلة».
- حفظ تلقائي للمسار والمسودّات + زر «متابعة من حيث توقفت».
- إدخال صوتي (ميكروفون) عبر Deepgram.

## 4. التصميم MOL TRANSPORT
- الهوية: **برتقالي/أصفر + أبيض**، بطاقات بيضاء، حواف دائرية كبيرة، أزرار عريضة مناسبة للهاتف.
- متغيرات الهوية معرّفة في `src/styles.css` تحت النطاق `.mol-brand`.
- ألوان دائماً عبر tokens دلالية — ممنوع hardcode مثل `bg-[#...]` أو `text-white` داخل المكوّنات.
- لوحة صاحب الشاحنة (`DriverDashboard.tsx`) مُغلّفة بـ `.mol-brand` لتكون متناسقة تماماً مع باقي واجهات التطبيق.
- **زر الرجوع:** كبير وواضح وثابت (sticky) في أعلى كل صفحة تحتاجه، عبر `AppHeader`، يرجع للصفحة الصحيحة بدون تسجيل خروج وبدون فقدان بيانات.
- **زر الرئيسية:** يعتمد على الدور (`useHomePath()` في `PhoneFrame.tsx`) ولا يسجّل الخروج.
- «إغلاق التطبيق» لا يمسح الجلسة.
- **لوحة صاحب الشاحنة:** أزرار التنقّل فقط في الهيدر؛ لا أزرار رجوع/رئيسية مكرّرة في وسط/أسفل الصفحة. زر «تحديث الطلبات» يعيد جلب الطلبات فعلياً من قاعدة البيانات.

## 5. أنواع الشاحنات
الصور في `src/assets/trucks/` (رسوم بيضاء واقعية، خلفية شفافة)، تُعرض في شبكة 4 أعمدة قابلة للاختيار، البطاقة كاملة قابلة للنقر، حالة الاختيار برتقالية، مع تلميح السعة:

| النوع | الملف |
|---|---|
| تريبورتور | `triporteur.png` |
| هوندا | `honda.png` |
| بيكوب | `pickup.png` |
| سطافيط | `staffit.png` |
| كونتير | `kontiri.png` |
| كاميون كبير | `camion.png` |
| رموك | `remorque.png` |
| كاميون لبان (Benne) | `benne.png` |

السعات تُشتق تلقائياً من النوع المختار (chips السعة)، بنفس الترتيب أعلاه.

اختيار نوع الشاحنة يتم **فقط** عبر خانات الصور (في طلب صاحب البضاعة، شاشة التسجيل، وصفحة الحساب «الشاحنة ديالك»). ممنوع إرجاع أزرار الطوناج (500 كلغ / 1 طن…) كواجهة اختيار؛ الحمولة القصوى تُحدَّد آلياً من النوع.

**الطلب المحلي:** بعد نجاح إرسال الطلب تُمسح بيانات الفورم والمسودة المحلية فقط، والطلب المنشور يبقى في قاعدة البيانات.

**GPS:** «استخدم موقعي الحالي» يستعمل الموقع الحقيقي فقط، مع إعادة محاولة بدقة عادية ورسائل واضحة عند رفض الإذن أو انتهاء المدة.

## 6. التسجيل وOTP والجلسة
- شاشات البداية (دخول/تسجيل موحد): اختيار الدور ← رقم الهاتف فقط ← شاشة OTP مستقلة ← إذا الرقم عنده حساب يدخل مباشرة، وإذا جديد يكمل التسجيل (الاسم + النسب + اللوحة/الشاحنة لصاحب الشاحنة) من نفس المسار. لا زر تسجيل مستقل.
- تلميح ثابت تحت الزر: «غادي توصلك رسالة SMS فيها كود التأكيد».
- OTP مطلوب فقط عند أول تسجيل بالرقم.
- إدارة الجلسة في `src/lib/hamoula-store.tsx` عبر `sessionState`: `checking | offline | authenticated | signed-out`.
  - جلسة صالحة أو قابلة للتجديد بالـ refresh token → دخول مباشر بلا SMS.
  - انقطاع الشبكة → `offline`: الحساب المحلي يبقى للعرض فقط، بدون اعتباره تسجيل دخول وبدون السماح بعمليات محمية.
  - جلسة ملغاة/توكن غير صالح → مسح الجلسة والحساب المحلي والعودة للتسجيل/OTP.
  - الحساب المحلي **لا** يتجاوز Auth أبداً.
- «خروج من الحساب» فقط هو ما يمسح الجلسة.
- مسار `/otp-test` متاح في التطوير فقط، وغير موجود في الإنتاج.

## 7. الصوت والميكروفون
- إملاء عبر Deepgram (`DEEPGRAM_API_KEY`) في `use-ai-dictation.ts`.
- **مهلة الصمت 2000ms**: إذا توقف المستخدم عن الكلام ثانيتين يتوقف التسجيل تلقائياً ويبدأ التحويل؛ إذا عاد للكلام قبلها يستمر التسجيل. النص الظاهر للمستخدم يقول «2 ثواني».
- أزرار ميكروفون برتقالية بنفس الحجم في: الاسم، النسب، رقم الهاتف، وحقول الطلب — النص المتعرّف عليه يُكتب مباشرة في الخانة الصحيحة.
- رسائل صوتية في الشات تُخزَّن في bucket `chat-voice`.

## 8. الخرائط وGPS
- Google Maps Platform عبر اتصال مُدار (managed connection) + Places Autocomplete للانطلاق والوجهة.
- عرض ماركرات الانطلاق/الوجهة، مسار متقطع، والمسافة بالكيلومتر.
- تتبع مباشر: `src/lib/hamoula-live-location.ts` (تحديث دوري، حركة سلسة، أثر المسار، دوران، سرعة، ETA، نسبة التقدم).
- إعادة ترتيب الشاحنات/الطلبات تلقائياً عند تغيّر إحداثيات GPS، مع ترتيب ثانوي ثابت (ID/تاريخ) وفولباك زمني عند غياب GPS.

## 9. البيانات والأمان
- الجداول: `app_users`, `loads`, `bids`, `chat_messages`, `push_subscriptions`, drafts.
- RLS صارم مبني على `auth.uid()` — ممنوع `USING(true)`.
- `chat-voice` محمي بـ `can_access_chat_load(load_id)`: طرفا المحادثة المصادقان فقط، بدون anon.
- لا صلاحية SELECT لـ anon على `push_subscriptions`.
- بيانات وهمية مكتوبة في الكود (`hamoula-demo-loads.ts`) معطّلة — تظهر البيانات الحقيقية فقط.
- نسخة احتياطية سابقة: `/mnt/documents/hamoula-backup-20260828/`.

## 10. ممنوع تغييره بدون طلب صريح
1. منطق Auth / OTP / إعدادات Vonage وSMS.
2. قاعدة البيانات، المخطط، وسياسات RLS والصلاحيات.
3. Google Maps / GPS / منطق التتبع.
4. الشات والرسائل الصوتية وPush Notifications.
5. Router والمسارات الحالية، و`/otp-test` يبقى تطويرياً فقط.
6. الفصل بين الدورين وترتيب النتائج حسب القرب.
7. هوية MOL TRANSPORT البرتقالية/البيضاء وصور الشاحنات وأسماؤها وترتيبها.
8. الدخول التجريبي بالرقم `0600000000` فقط، وفي التطوير فقط.
9. مهلة الميكروفون 2000ms والنص الظاهر «2 ثواني».
10. سلوك الجلسة الدائمة: «إغلاق التطبيق» لا يخرج، «خروج من الحساب» فقط يخرج.
11. عدم إرجاع صفحة العروض القديمة.
12. ملفات Lovable Cloud المولّدة تلقائياً (`src/integrations/supabase/*`, `.env`).

## 11. طريقة العمل
- قبل أي تعديل: قراءة `PROJECT_MEMORY.md` و`CHANGELOG.md` وProject Knowledge.
- بعد كل تعديل ناجح: تحديث الذاكرة وCHANGELOG.
- عند تعارض طلب جديد مع قرار سابق: التوقف وسؤال المالك قبل التنفيذ.

## الهوية اللونية العامة (محدَّث)
هوية MOL TRANSPORT (برتقالي/أصفر دافئ/أبيض) أصبحت التوكنات الافتراضية في `:root` داخل `src/styles.css`، فتنطبق على كل الصفحات والمكونات تلقائياً (لوحة صاحب الشاحنة، الطلبات، العروض، الشات، التتبع، الإعدادات...). الكلاس `.mol-brand` باقٍ للتوافق فقط.
ألوان الخرائط: الماركر والمسار برتقالي `#f97316`. استثناء وحيد: `--whatsapp` يبقى أخضر لأنه لون علامة واتساب.

## قواعد UI إضافية (2026-08-28)
- الهيدر يستعمل شبكة `grid-cols-[minmax(0,1fr)_auto]` مع `min-w-0`/`shrink-0`، وبطاقة الحساب محدودة العرض مع `truncate`.
- الألوان المحايدة (border/muted/input) دافئة (hue ~80) وليست رمادية مزرقة.
- صفوف الفلاتر الأفقية تستعمل أداة `scroll-row` لإخفاء شريط التمرير.

## 2026-08-28 — زر «إغلاق التطبيق» وتداخل ScrollFab
- شاشة اختيار الدور فيها زر «إغلاق التطبيق» أعلى الصفحة: يغلق الواجهة فقط (`window.close` + فولباك `about:blank`)، ممنوع يربط بأي تسجيل خروج أو مسح جلسة.
- محتوى شاشة اختيار الدور عنده هامش سفلي `pb-28` باش زر التمرير العائم (`ScrollFab`) ما يغطيش منطقة «الدخول التجريبي».

## 2026-08-29 — الصفحة الرئيسية النهائية (مرجع MOL)
- شاشة اختيار الدور مطابقة لمرجع MOL: خلفية كريمية/ذهبية مع skyline مغربي (`src/assets/mol-skyline.png`) وشمس ذهبية، شعار «مول ترانسبور» + «نقل البضائع بسهولة وأمان»، صف 6 شاحنات بيضاء من الصغيرة للكبيرة، Card أبيض «اختر نوع الحساب» فيه «صاحب بضاعة» و«صاحب شاحنة» مع أزرار «دخول».
- «الدخول التجريبي» روابط نصية صغيرة غير بارزة أسفل الصفحة (وضع التطوير فقط) — ممنوع إرجاعه كأزرار كبيرة بارزة.
- زر «إغلاق التطبيق» يبقى صغيراً ومندمجاً بصرياً في الأعلى.
