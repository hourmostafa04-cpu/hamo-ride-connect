# CHANGELOG — MOL TRANSPORT / مول طرانسبور

سجل التغييرات المهمة (من الأقدم إلى الأحدث). التواريخ تقريبية حسب مسار العمل.

## الإعداد الأولي
- تحديث السر `DEEPGRAM_API_KEY` وربط اتصال مُدار بـ Google Maps Platform.
- التحقق من عرض نقاط الانطلاق/الوجهة على الخريطة (Casablanca ↔ Marrakech، مسافة 219 كم) وPlaces Autocomplete.

## التتبع المباشر
- إضافة `src/lib/hamoula-live-location.ts`: تحديثات GPS دورية وحركة سلسة للماركر.
- تحديث `TripMap.tsx` و`tracking.tsx`: أثر المسار، الدوران، السرعة، ETA، نسبة التقدم.
- إضافة زر «إنهاء الرحلة»: يوقف التتبع، ينهي الرحلة، صوت نجاح، ثم `/trip-details`.

## فصل الأدوار والتنقّل
- حذف قسم العروض القديم لصاحب البضاعة؛ يرى الشاحنات فقط مرتبة حسب المسافة.
- صاحب الشاحنة يرى الطلبات فقط مرتبة حسب القرب.
- أزرار رجوع كبيرة وثابتة عبر `AppHeader` في الصفحات المحتاجة (منها `OrderForm.tsx` و`DriverDashboard.tsx`).
- إعادة ترتيب تلقائية عند تغيّر GPS مع ترتيب ثانوي ثابت وفولباك زمني.
- إصلاح زر «الرئيسية» عبر `useHomePath()` في `PhoneFrame.tsx` (حسب الدور، بدون تسجيل خروج).

## الوضع التجريبي
- `src/lib/demo-login.ts` + أزرار «دخول تجريبي» في التطوير فقط؛ الرقم `0600000000` فقط يتخطى SMS.

## الميكروفون
- توحيد مهلة الصمت على 2000ms في `use-ai-dictation.ts`.
- تصحيح النص الظاهر من «4.5 ثواني» إلى «2 ثواني».
- إضافة ميكروفون لحقلي «الاسم» و«النسب» بنفس تصميم حقل الهاتف مع كتابة النص في الخانة الصحيحة.

## الحفظ والاستئناف
- `src/lib/last-route.ts` + `ResumeWhereYouLeft.tsx`: حفظ الصفحة/المرحلة والبيانات في localStorage وزر «متابعة من حيث توقفت».
- إصلاح حفظ مسودة الطلب، وعدّاد العروض في `MyBids.tsx`، وتصفية المسارات حسب الدور.

## الشات
- شات حقيقي بين صاحب البضاعة وصاحب الشاحنة فقط: `chat_messages`، bucket `chat-voice`، `src/lib/hamoula-chat.ts`، شارات ومداخل داخل صفحات الطلب/الاتصال.

## الأمان
- استبدال سياسات `USING(true)` بسياسات صارمة مبنية على `auth.uid()`.
- تأمين bucket `chat-voice` عبر `can_access_chat_load(load_id)` ومنع anon.
- سحب صلاحية SELECT من anon على `push_subscriptions`.

## الإشعارات
- Web Push عبر PWA: `public/sw.js`، جدول `push_subscriptions`، إشعارات الطلبات والعروض والشات وردود العروض.
- عرض السائقين المقدِّمين للعروض داخل «طلباتي» مع قبول/رفض مرتبطين بـ `acceptBid`/`declineBid`.

## Router
- نقل `/otp-test` إلى تحميل ديناميكي وتسجيل شرطي في التطوير فقط.
- إصلاح `src/router.tsx` بحيث لا يمسح `addChildren` المسارات الأصلية في وضع DEV.

## نشر الطلبات والبيانات
- تنظيف بيانات الاختبار مع نسخة احتياطية في `/mnt/documents/hamoula-backup-20260828/`.
- إصلاح `publishLoad` في `hamoula-store.tsx`: بناء الطلب خارج تحديث الحالة، حفظ عبر `saveLoad()` قبل اعتباره منشوراً، وأخطاء واضحة بدون ابتلاع.
- تعطيل الطلبات التجريبية المكتوبة في `hamoula-demo-loads.ts`.

## الهوية الجديدة MOL TRANSPORT
- تغيير الاسم الظاهر إلى «مول طرانسبور» في العنوان وشاشة البداية والهيدر و`manifest.webmanifest` و`sw.js`.
- تصميم برتقالي/أبيض لأول 3 شاشات: اختيار الدور، الاسم/النسب/الهاتف، شاشة OTP مستقلة؛ متغيرات `.mol-brand` في `src/styles.css`.
- تحديث واجهة صاحب البضاعة (`OrderForm.tsx`): ملاحظات، تلميحات السعة، اختيار البطاقة كاملة، بطاقة «شحن آمن ومضمون»، شرح السعر.
- إنشاء 8 صور شاحنات في `src/assets/trucks/` وعرضها في شبكة 4 أعمدة.
- واجهة صاحب الشاحنة: حقل رقم اللوحة + تلميح SMS، ثم OTP، ثم اختيار نوع الشاحنة بالصور.

## الجلسة
- حفظ الحساب محلياً والتمييز بين الخروج الصريح وإعادة التحميل.
- تصحيح استرجاع الجلسة عبر `sessionState` (`checking | authenticated | offline | signed-out`) مع تجديد refresh token، وعدم استعمال الحساب المحلي لتجاوز Auth.

## النشر
- نشر النسخة على الرابط الثابت: https://hamo-ride-connect.lovable.app

## 2026-08-28 — نظام الذاكرة
- إنشاء `PROJECT_MEMORY.md` و`CHANGELOG.md` وتحديث Project Knowledge بالقواعد الثابتة. لا تغيير في الكود أو التصميم أو Backend.

## 2026-08-28 — تصميم لوحة صاحب الشاحنة
- تطبيق هوية MOL TRANSPORT (برتقالي/أصفر/أبيض) على `DriverDashboard.tsx` عبر تغليف الصفحة بـ `.mol-brand`.
- الحفاظ على نفس ترتيب العناصر والنصوص والأزرار ووظائف GPS والطلب الصوتي وقائمة الطلبات.

## 2026-08-28 — توحيد الهوية اللونية (CSS/UI فقط)
- تحويل التوكنات العامة في `src/styles.css` من الأخضر إلى هوية MOL TRANSPORT (برتقالي/أصفر دافئ/أبيض): `--primary`, `--primary-glow`, `--primary-soft`, `--secondary`, `--secondary-foreground`, `--accent`, `--accent-foreground`, `--ring`, `--foreground`, وظل `shadow-soft`.
- تحويل ألوان الخرائط من `#16a34a` إلى `#f97316` في `MapPicker.tsx` و`TripMap.tsx` و`NearbyDriversMap.tsx` (الماركر، المسار، الهالة).
- تحويل ألوان ورقة تصدير العروض `src/lib/bids-export.ts` إلى درجات دافئة.
- `--whatsapp` بقي أخضر لأنه لون علامة واتساب.
- لا تغيير في النصوص أو ترتيب الصفحات أو أي منطق/Backend.

## 2026-08-28 — مراجعة UI/UX عامة (CSS/UI فقط)
- إصلاح هيدر التطبيق `PhoneFrame.tsx`: شبكة `grid-cols-[minmax(0,1fr)_auto]` مع `min-w-0` و`shrink-0` حتى ما يتقطعش المحتوى على الهواتف الصغيرة، وتصغير حجم العنوان على الموبايل (`text-xl sm:text-2xl`).
- إصلاح بطاقة الحساب `ProfileSwitcher.tsx`: عرض أقصى محدود + `truncate` للاسم والدور + `shrink-0` للأفاتار والسهم (كان الاسم كيتقطع على 3 أسطر).
- تدفئة التوكنات المحايدة في `src/styles.css` (`--muted`, `--muted-foreground`, `--border`, `--input`, `--card-foreground`, `--popover-foreground`) من الرمادي المزرق إلى رمادي دافئ متناسق مع هوية MOL.
- إضافة أداة `scroll-row` لإخفاء شريط التمرير في صفوف الفلاتر الأفقية وتطبيقها في `DriverDashboard.tsx` و`MyBids.tsx` و`NearbyDrivers.tsx`.
- لا تغيير في النصوص ولا الوظائف ولا Auth/OTP/Vonage ولا Maps/GPS ولا Voice/Chat/Push ولا الطلبات/العروض ولا قاعدة البيانات.

## 2026-08-28 — الصفحة الرئيسية: زر إغلاق وتداخل السهم (UI فقط)
- إضافة زر «إغلاق التطبيق» أعلى شاشة اختيار الدور في `RegisterScreen.tsx`: يغلق الواجهة فقط (`window.close` مع فولباك `about:blank`)، لا يمسح الجلسة ولا يسجل خروجاً.
- إصلاح تداخل زر التمرير العائم البرتقالي (`ScrollFab`) مع منطقة «الدخول التجريبي» بهامش سفلي إضافي (`pb-28`).
- النص «شكون نتا؟» كان مصححاً في الكود؛ النشر الجديد يحدّث النسخة المنشورة.
- لا تغيير في Auth/OTP/Vonage/الجلسة/قاعدة البيانات/RLS/GPS/Maps/Chat/Voice/Push/الطلبات/العروض.

- %Y-%m-%d: إعادة تصميم الصفحة الرئيسية (اختيار الحساب) حسب مرجع MOL: خلفية كريمية/ذهبية، شعار MOL TRANSPORT، صف شاحنات من الصغيرة للكبيرة، زخرفة أفق مغربي (src/assets/mol-skyline.png)، بطاقة «اختر نوع الحساب» بترتيب صاحب بشاحنة ثم صاحب بضاعة مع أزرار «دخول»، وزر «إغلاق التطبيق» صغير في الأعلى. UI فقط.

## 2026-08-29 — الصفحة الرئيسية: تثبيت شكل المرجع (UI فقط)
- مطابقة نهائية لمرجع MOL: عنوان «مول ترانسبور» مع أيقونة شاحنة، شعار «نقل البضائع بسهولة وأمان»، 6 صور نقل بيضاء من الصغيرة للكبيرة، skyline مغربي (mol-skyline.png) بشفافية خفيفة، شمس ذهبية وزخرفة قطرية في الأعلى.
- Card «اختر نوع الحساب» أبيض بحواف دائرية: «صاحب بضاعة» (أيقونة صندوق، «أبحث عن شاحنة لنقل بضاعتي إلى وجهتها») و«صاحب شاحنة» (أيقونة شاحنة، «أبحث عن شحنات لنقلها بشاحنتي»)، ولكل منهما زر «دخول».
- «الدخول التجريبي» أصبح روابط نصية صغيرة غير بارزة أسفل الصفحة (يبقى وضع التطوير فقط)، وزر «إغلاق التطبيق» صغير ومندمج بصرياً في الأعلى.
- لا تغيير في Auth/OTP/Vonage/الجلسة/قاعدة البيانات/RLS/GPS/Maps/Chat/Voice/Push/الطلبات/العروض.

## 2026-08-29 — دخول/تسجيل موحد في الصفحة الرئيسية
- RegisterScreen.tsx: صفحة «دخول / تسجيل» تطلب رقم الهاتف فقط (حُذفت حقول الاسم/النسب/اللوحة منها)؛ بعد OTP: حساب موجود → دخول مباشر، حساب جديد → يكمل التسجيل في نفس المسار. لا زر تسجيل مستقل. نفس Auth/OTP دون تغيير.

## 2026-08-29 — تنظيف أزرار لوحة صاحب الشاحنة (UI فقط)
- `DriverDashboard.tsx`: إبقاء أزرار التنقّل فقط في `AppHeader` (رجوع + رئيسية + حساب).
- حذف زر «الرجوع للرئيسية» المكرّر من وسط الصفحة.
- حذف رابط «الرئيسية» المكرّر في أسفل الصفحة.
- زر «تحديث الطلبات» أصبح يستدعي `refreshBoard()` فعلياً لإعادة جلب الطلبات من قاعدة البيانات.
- لا تغيير في Backend أو Auth أو DB أو منطق الطلبات/العروض.

## 2026-09-09
- حساب صاحب الشاحنة: حذف أزرار الطوناج من «الشاحنة ديالك» (account) ومن شاشة التسجيل، والتعويض بخانات صور أنواع الشاحنات الثمانية (نفس الصور والترتيب والسعات)؛ الاختيار كيحدّث نوع الشاحنة والحمولة القصوى تلقائياً.
- طلب صاحب البضاعة: بعد نجاح «إرسال الطلب» كيتمسح الفورم المحلي والمسودة المخزّنة (resetForm + discardDraft)، والطلب المنشور كيبقى فقاعدة البيانات.
- GPS: زر «استخدم موقعي الحالي» ولّى كيطلب الإذن، وكيعاود المحاولة بدقة عادية عند الفشل، وكيعرض رسائل واضحة (إذن مرفوض / انتهت المدة / خطأ)، بلا أي موقع تجريبي.

## 2026-09-09 — تصفير فورم الطلب بعد الإرسال + الثمن حسب طوناج الشاحنة
- `hamoula-store.tsx`: بعد نجاح `publishLoad` (بعد INSERT) يُصفّر `board.request` لطلب فارغ جديد مباشرة، وعلم `publishingRef` يمنع سباق حفظ المسودة (debounce 700ms) من إعادة حفظ مسودة قديمة بعد المسح.
- `OrderForm.tsx`: الثمن التقديري يستعمل طوناج الشاحنة المختارة (`truckMaxKg`) عند غياب حمولة محددة — كونتير=8طن، إلخ — بنفس جدول التسعير الموجود (`estimatePrice`/`weightFactor`) بلا أسعار جديدة. النص التوضيحي يذكر السعة.

## 2026-09-10 — تصفير اختيار الشاحنة بعد نشر الطلب
- `src/lib/order-form-state.ts`: `emptyOrderForm()` يبدأ الآن بـ `truck: ""` (لا شاحنة محددة مسبقاً).
- `src/lib/hamoula-store.tsx`: `defaultRequest.truck` أصبح `""` أيضاً لضمان تناسق الحالة المخزّنة.
- `src/components/hamoula/OrderForm.tsx`: لا يُعرض ثمن تقديري ولا تفاصيل شاحنة افتراضية قبل الاختيار؛ بعد نجاح «إرسال الطلب» جميع الـ 8 كروت الشاحنات ترجع غير محددة، و«طلب جديد» يبدأ بفورم فارغ تماماً.
