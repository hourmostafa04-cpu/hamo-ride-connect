# CHANGELOG — MOL TRANSPORT / مول طرانسبور

سجل التغييرات المهمة (من الأقدم إلى الأحدث). التواريخ تقريبية حسب مسار العمل.

## الإعداد الأولي
- تحديث السر `DEEPGRAM_API_KEY` وربط اتصال مُدار بـ Google Maps Platform.
- التحقق من عرض نقاط الانطلاق/الوجهة على الخريطة (Casablanca ↔ Marrakech، مسافة 219 كم) وPlaces Autocomplete.

## التتبع المباشر
- إضافة `src/lib/hamoula-live-location.ts`: تحديثات GPS دورية وحركة سلسة للماركر.
- تحديث `TripMap.tsx` و`tracking.tsx`: أثر المسار، الدوران، السرعة، ETA، نسبة التقدم.
- إضافة زر «إنهاء الرحلة»: يوقف التتبع، ينهي الرحلة، صوت نجاح، ثم `/trip-details`.

## فصل الأدوار والتنقّل
- حذف قسم العروض القديم لصاحب البضاعة؛ يرى الشاحنات فقط مرتبة حسب المسافة.
- صاحب الشاحنة يرى الطلبات فقط مرتبة حسب القرب.
- أزرار رجوع كبيرة وثابتة عبر `AppHeader` في الصفحات المحتاجة (منها `OrderForm.tsx` و`DriverDashboard.tsx`).
- إعادة ترتيب تلقائية عند تغيّر GPS مع ترتيب ثانوي ثابت وفولباك زمني.
- إصلاح زر «الرئيسية» عبر `useHomePath()` في `PhoneFrame.tsx` (حسب الدور، بدون تسجيل خروج).

## الوضع التجريبي
- `src/lib/demo-login.ts` + أزرار «دخول تجريبي» في التطوير فقط؛ الرقم `0600000000` فقط يتخطى SMS.

## الميكروفون
- توحيد مهلة الصمت على 2000ms في `use-ai-dictation.ts`.
- تصحيح النص الظاهر من «4.5 ثواني» إلى «2 ثواني».
- إضافة ميكروفون لحقلي «الاسم» و«النسب» بنفس تصميم حقل الهاتف مع كتابة النص في الخانة الصحيحة.

## الحفظ والاستئناف
- `src/lib/last-route.ts` + `ResumeWhereYouLeft.tsx`: حفظ الصفحة/المرحلة والبيانات في localStorage وزر «متابعة من حيث توقفت».
- إصلاح حفظ مسودة الطلب، وعدّاد العروض في `MyBids.tsx`، وتصفية المسارات حسب الدور.

## الشات
- شات حقيقي بين صاحب البضاعة وصاحب الشاحنة فقط: `chat_messages`، bucket `chat-voice`، `src/lib/hamoula-chat.ts`، شارات ومداخل داخل صفحات الطلب/الاتصال.

## الأمان
- استبدال سياسات `USING(true)` بسياسات صارمة مبنية على `auth.uid()`.
- تأمين bucket `chat-voice` عبر `can_access_chat_load(load_id)` ومنع anon.
- سحب صلاحية SELECT من anon على `push_subscriptions`.

## الإشعارات
- Web Push عبر PWA: `public/sw.js`، جدول `push_subscriptions`، إشعارات الطلبات والعروض والشات وردود العروض.
- عرض السائقين المقدِّمين للعروض داخل «طلباتي» مع قبول/رفض مرتبطين بـ `acceptBid`/`declineBid`.

## Router
- نقل `/otp-test` إلى تحميل ديناميكي وتسجيل شرطي في التطوير فقط.
- إصلاح `src/router.tsx` بحيث لا يمسح `addChildren` المسارات الأصلية في وضع DEV.

## نشر الطلبات والبيانات
- تنظيف بيانات الاختبار مع نسخة احتياطية في `/mnt/documents/hamoula-backup-20260828/`.
- إصلاح `publishLoad` في `hamoula-store.tsx`: بناء الطلب خارج تحديث الحالة، حفظ عبر `saveLoad()` قبل اعتباره منشوراً، وأخطاء واضحة بدون ابتلاع.
- تعطيل الطلبات التجريبية المكتوبة في `hamoula-demo-loads.ts`.

## الهوية الجديدة MOL TRANSPORT
- تغيير الاسم الظاهر إلى «مول طرانسبور» في العنوان وشاشة البداية والهيدر و`manifest.webmanifest` و`sw.js`.
- تصميم برتقالي/أبيض لأول 3 شاشات: اختيار الدور، الاسم/النسب/الهاتف، شاشة OTP مستقلة؛ متغيرات `.mol-brand` في `src/styles.css`.
- تحديث واجهة صاحب البضاعة (`OrderForm.tsx`): ملاحظات، تلميحات السعة، اختيار البطاقة كاملة، بطاقة «شحن آمن ومضمون»، شرح السعر.
- إنشاء 8 صور شاحنات في `src/assets/trucks/` وعرضها في شبكة 4 أعمدة.
- واجهة صاحب الشاحنة: حقل رقم اللوحة + تلميح SMS، ثم OTP، ثم اختيار نوع الشاحنة بالصور.

## الجلسة
- حفظ الحساب محلياً والتمييز بين الخروج الصريح وإعادة التحميل.
- تصحيح استرجاع الجلسة عبر `sessionState` (`checking | authenticated | offline | signed-out`) مع تجديد refresh token، وعدم استعمال الحساب المحلي لتجاوز Auth.

## النشر
- نشر النسخة على الرابط الثابت: https://hamo-ride-connect.lovable.app

## 2026-08-28 — نظام الذاكرة
- إنشاء `PROJECT_MEMORY.md` و`CHANGELOG.md` وتحديث Project Knowledge بالقواعد الثابتة. لا تغيير في الكود أو التصميم أو Backend.

## 2026-08-28 — تصميم لوحة صاحب الشاحنة
- تطبيق هوية MOL TRANSPORT (برتقالي/أصفر/أبيض) على `DriverDashboard.tsx` عبر تغليف الصفحة بـ `.mol-brand`.
- الحفاظ على نفس ترتيب العناصر والنصوص والأزرار ووظائف GPS والطلب الصوتي وقائمة الطلبات.

## 2026-08-28 — توحيد الهوية اللونية (CSS/UI فقط)
- تحويل التوكنات العامة في `src/styles.css` من الأخضر إلى هوية MOL TRANSPORT (برتقالي/أصفر دافئ/أبيض): `--primary`, `--primary-glow`, `--primary-soft`, `--secondary`, `--secondary-foreground`, `--accent`, `--accent-foreground`, `--ring`, `--foreground`, وظل `shadow-soft`.
- تحويل ألوان الخرائط من `#16a34a` إلى `#f97316` في `MapPicker.tsx` و`TripMap.tsx` و`NearbyDriversMap.tsx` (الماركر، المسار، الهالة).
- تحويل ألوان ورقة تصدير العروض `src/lib/bids-export.ts` إلى درجات دافئة.
- `--whatsapp` بقي أخضر لأنه لون علامة واتساب.
- لا تغيير في النصوص أو ترتيب الصفحات أو أي منطق/Backend.
